#include <bits/stdc++.h>
#define mod 1000000007
#define lli long long int
using namespace std;

int main(){
    lli g;
    cin>>g;
    while(g--){
        lli breadth,height;
        cin>>breadth>>height; 
        vector<lli> arr(g);
        for(int i=0;i<g;i++){
            int l;
            cin>>l;
            arr.push_back(l);
        }

    }

    return 0;
}