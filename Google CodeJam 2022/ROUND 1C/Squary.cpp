#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll ts=1,te;
    cin>>te;
    while(ts<=te)
    {
        ll n;
        cin >> n;
        
        ts++;
    }

    return 0;
}