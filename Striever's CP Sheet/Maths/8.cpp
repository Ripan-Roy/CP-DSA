#include <bits/stdc++.h>
#define ll long long 
using namespace std;

int main(){
    ll t;
    cin>>t;
    while(t--)
    {
        vector<ll> arr(3);
        for (int i = 0; i < 3; i++)
        {
            cin >> arr[i];
        }
        
    }

    return 0;
}