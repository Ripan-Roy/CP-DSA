#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        double num;
        cin >> num;
        cout << ceil(num / 10) << "\n";
    }
    return 0;
}