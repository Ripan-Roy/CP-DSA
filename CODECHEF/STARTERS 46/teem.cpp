#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    ll ts = 1;
    ll te;
    cin >> te;
    while (ts <= te)
    {
        ll n;
        cin >> n;
        vector<ll> arr1(n);
        ll m;
        cin >> m;
        vector<ll> arr2(m);
        for (int i = 0; i < n; i++)
        {
            cin >> arr1[i];
        }
        for (int i = 0; i < m; i++)
        {
            cin >> arr2[i];
        }
        ll k;
        cin >> k;
        ll ans = 0;

        ts++;
    }
    return 0;
}