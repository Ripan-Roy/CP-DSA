#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    ll t;
    cin >> t;
    while (t--)
    {
        ll maxT, maxN, sumN;
        cin >> maxT >> maxN >> sumN;
        ll t = maxN / maxT;
        
    }
    return 0;
}