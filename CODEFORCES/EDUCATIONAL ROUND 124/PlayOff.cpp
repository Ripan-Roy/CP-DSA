#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        cout <<(long long)(pow(2,n)-1)<<"\n";
        // for (int i = 1; i <= n; i++)
        // {
        //     cout << arr[i] << " ";
        // }
        // cout << "\n";
    }

    return 0;
}