#include <bits/stdc++.h>
#define lli long long int
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        lli n;
        cin >> n;
        vector<lli> arr(n);
        for (lli i = 0; i < n; i++)
        {
            cin >> arr[i];
        }
        

    }

    return 0;
}